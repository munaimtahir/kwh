# EMT — Stage 0 Master Prompt (Bootstrap & Code Import)

> Role: Autonomous AI developer. Create a clean monorepo, add root configs, basic docs, and prep for later stages.
> Do not build features in Stage 0.

## Tasks
1) Initialize repo & layout
2) Add `.gitignore`, `.editorconfig`, `.gitattributes`, `README.md`
3) Create `docs/progress/stage0.md`; (optionally) add more roadmap docs
4) Add `setup.sh` and `scripts/smoke.sh`; document them in `agents.md`
5) Open a PR `chore(stage0): bootstrap monorepo and docs`

## DoD
- Tree matches target structure
- README explains modules & next stages
- Setup + smoke run without interaction
- PR merged
