<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Notifications (Android 13+) if your app posts reminders -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.Kwh">

        <!-- If you have a custom Application class, set android:name here -->
        <!-- android:name=".KwhApp" -->

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- (Optional) If you use a BroadcastReceiver for snooze, declare it here
        <receiver
            android:name=".reminders.SnoozeReceiver"
            android:exported="false" />
        -->

    </application>
</manifest>
